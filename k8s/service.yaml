apiVersion: v1
kind: Service
metadata:
  name: devops-flex-api
  namespace: default
spec:
  selector:
    app: devops-flex-api
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080
  type: LoadBalancer
